import React from "react";
import RequestCard from "../requestCard";
const Requests = () => {
  return (
    <div>
      <RequestCard />
      <RequestCard />
      <RequestCard />
    </div>
  );
};

export default Requests;

