import React from "react";
import ResponseCard from "../responseCard";
const Responses = () => {
  return (
    <div>
      <ResponseCard />
      <ResponseCard />
      <ResponseCard />
    </div>
  );
};

export default Responses;
